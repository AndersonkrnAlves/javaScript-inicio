const nota1 = 50
const nota2 = 80
const nota3 = 50

const resultado = nota1 + nota2 + nota3
const media = resultado / 3
const ExibirResultado = `A média das Notas: ${nota1}, ${nota2} e ${nota3} é ${media}  `
//console.log('A média dos numeros: ', number1, number2, number3, ' é ', media)
console.log(ExibirResultado)